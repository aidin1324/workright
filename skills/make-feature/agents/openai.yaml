interface:
  display_name: "Make Feature"
  short_description: "Build features safely from discovery to proof"
  default_prompt: "Use $make-feature to design, implement, and verify this feature end to end."
policy:
  allow_implicit_invocation: false
